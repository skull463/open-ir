# `@bb/db-core/src`

Provider-agnostic interfaces and shared types for the document database layer.

## Files

- **index.ts** — all public interfaces (`IDocumentDatabaseProvider`, `IKnowledgeRepository`, `IRawRepository`, etc.) and shared types (`FileAnalysis`, `RawFileDoc`, `DbPingResult`)
