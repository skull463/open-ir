/**
 * Bytebell brand accent — purple. Single source of truth for the TUI accent
 * used by cursors, active rows, form highlights, and the control-key caps.
 * A hex value so it renders identically as both foreground and background on
 * truecolor terminals.
 */
export const ACCENT = "#a855f7";
