# `@bb/graph-core/src`

Provider-agnostic interfaces and shared types for the graph database layer.

## Files

- **index.ts** — all public interfaces (`IGraphDatabaseProvider`, `IGraphKnowledgeRepository`, `IGraphFileRepository`, etc.), input/output types (`NodeScope`, `UpsertFileNodeInput`, `UpsertFolderNodeInput`, etc.), and `GraphPingResult`
