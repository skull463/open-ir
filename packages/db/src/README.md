# `@bb/db/src`

Provider registry, facade, and lifecycle management for the document database layer.

## Files

- **index.ts** — provider registry (`registerDbProvider`, `connectDb`, `closeDb`, `getDb`), convenience facade objects (`knowledge`, `raw`, `stats`, `activity`, `usage`), and `pingDb()`
